#include <bits/stdc++.h>
using namespace std;

int wx,wy;
int bx,by;
int dirx,diry;
int power;

int main()
{
    cin>>wx>>wy;
    cin>>bx>>by;
    cin>>dirx>>diry;
    cin>>power; //x=[0,16]; y=[0,5]
    int flag=1; //1=white;
    int ans=-2;
    while(power>0)
    {
        if(dirx==-1 && diry==-1)
        {
            if(flag==)
            
        }
        else if(dirx==-1 && diry==1)
        {
            
        }
        else if(dirx==1 && diry==-1)
        {
            
        }
        else if(dirx==1 && diry==1)
        {
            
        }



    }

    return 0;
}
